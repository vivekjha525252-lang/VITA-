# VITA-

A lightweight website for people researching world markets with only a few keywords.

## Run locally

Because this project is static HTML/CSS/JS, you can run it with any local server:

```bash
python3 -m http.server 8080
```

Then open `http://localhost:8080`.
